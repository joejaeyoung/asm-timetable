# Base URL of the Spring Boot backend API
# In development, the default fallback (http://localhost:8080) is used when this is unset.
# In production builds, set this to your deployed backend URL before running `npm run build`.
VITE_API_URL=http://localhost:8080
